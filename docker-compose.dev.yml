version: '3.9'
services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --log-level debug
    environment:
      - APP_ENV=development
      - LOG_LEVEL=DEBUG
  frontend:
    command: npm run dev
